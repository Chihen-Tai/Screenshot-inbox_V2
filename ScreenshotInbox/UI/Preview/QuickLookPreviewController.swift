import AppKit
import Quartz

/// Bridges Quick Look (`QLPreviewPanel`) into the app for spacebar preview.
/// Phase 2 will conform to `QLPreviewPanelDataSource` / `QLPreviewPanelDelegate`.
final class QuickLookPreviewController: NSObject {
    // TODO: presentPanel(for screenshots: [Screenshot])
}
