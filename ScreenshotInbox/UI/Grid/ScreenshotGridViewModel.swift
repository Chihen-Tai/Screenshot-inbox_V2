import Foundation

/// Phase-2 prototype keeps grid state in `AppState`. This view model is
/// intentionally thin and exists for Phase 3 to grow into.
@MainActor
final class ScreenshotGridViewModel: ObservableObject {
    // TODO Phase 3: own paging, search debouncing, and infinite scroll cursors.
}
