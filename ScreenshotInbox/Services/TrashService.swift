import Foundation

/// In-app trash with restore + permanent delete.
final class TrashService {
    // TODO: soft-delete flag, expose Trash listing, permanent delete with file removal.
    init() {}
}
